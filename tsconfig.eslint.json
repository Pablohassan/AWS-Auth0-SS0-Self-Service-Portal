{
  "extends": "./tsconfig.json",
  "include": [
    // repeated from base config's "include" setting
    "src",
    "tests",
    "./src",
    "*.ts",
    "*.tsx",
    // these are the eslint-only inclusionsnp
    ".eslintrc.js"
  ]
}
